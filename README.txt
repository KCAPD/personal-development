KCA Mobile Optimisation — CSS-only release

Replace ONLY style.css in the repository root.

This release:
- keeps year buttons in a 2 x 3 grid on phones
- keeps milestone cards in two columns
- keeps value-character cards in two columns
- improves hero spacing and character placement
- preserves all HTML and JavaScript
- uses one column only for the long-form character pop-up

After committing, hard-refresh the live page with Cmd + Shift + R.
